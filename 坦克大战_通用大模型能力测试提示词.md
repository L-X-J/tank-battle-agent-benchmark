# 《坦克大战》完整实现——通用大模型能力测试提示词

下面这份提示词适合直接交给 **Codex、Claude Code、OpenCode、Cursor、Gemini CLI、Reasoning Agent 或普通聊天模型**。它不仅测试代码生成，还会测试项目规划、游戏机制、碰撞系统、AI、架构设计、自动测试、调试和交付能力。

复制下面的全部内容即可。

```text
# 坦克大战完整游戏实现基准任务

你现在是该项目唯一的：

- 游戏设计师
- 前端工程师
- 游戏引擎工程师
- AI 工程师
- 测试工程师
- 交付负责人

你的任务不是编写示例、概念验证或半成品，而是从零完成一个真正可以运行和游玩的经典《坦克大战 / Battle City》核心玩法模拟游戏。

游戏应保留经典坦克大战的主要机制，但必须使用原创名称、原创关卡、原创图形和程序合成音效。禁止复制 Nintendo、Namco 或其他商业作品的受版权保护图片、音乐、地图和商标。

---

# 一、执行方式

在当前工作目录下创建：

```text
tank-battle-benchmark/
```

所有项目文件必须写入该目录。

你必须直接完成以下工作：

1. 创建完整项目文件；
2. 实现全部核心玩法；
3. 创建自动化测试；
4. 实际运行测试；
5. 修复发现的问题；
6. 启动本地服务器进行运行验证；
7. 检查浏览器控制台错误；
8. 编写 README 和最终验收报告。

不要只回复计划。

不要在创建几个空文件后停止。

不要要求用户继续补充需求。

不要询问用户是否同意某个技术方案。

遇到技术问题时，由你自行选择替代方案继续完成。

如果某个工具不可用，应切换到其他可行方法，而不是中止整个任务。

不得删除或修改项目目录之外的任何文件。

如果当前环境确实没有文件写入和代码执行能力，则必须完整输出每一个项目文件的内容，并将最终状态标记为 `TOOL_LIMITED`，不得只输出伪代码。

---

# 二、固定技术栈

为保证不同模型的结果可以公平比较，技术栈固定如下：

```text
HTML5
CSS3
原生 JavaScript ES2022
Canvas 2D
Web Audio API
Node.js
node:test
```

约束：

- 不使用 React、Vue、Angular、Phaser、PixiJS 或其他游戏框架；
- 不使用 TypeScript；
- 不使用 npm 第三方依赖；
- 不使用 CDN；
- 不请求网络资源；
- 不使用外部图片、字体、音乐或音效文件；
- 所有图形必须由 Canvas 程序绘制；
- 所有音效必须由 Web Audio API 程序合成；
- 项目必须能够完全离线运行；
- 不需要构建、编译或打包步骤；
- 使用 Node.js 自带模块实现本地静态服务器；
- 使用 Node.js 自带的 `node:test` 实现测试。

必须支持：

```bash
npm start
npm test
```

其中：

```text
npm start
```

应启动本地服务器，并在终端中输出可访问地址，例如：

```text
http://localhost:8080
```

---

# 三、游戏基础规格

## 3.1 游戏画面

采用像素风格 Canvas 渲染。

建议逻辑尺寸：

```text
地图区域：416 × 416
地图网格：26 × 26
基础格尺寸：16 × 16
坦克尺寸：32 × 32
右侧信息栏：96 像素左右
```

最终 Canvas 可以根据窗口大小等比例放大或缩小，但必须：

- 保持固定宽高比；
- 保持像素边缘清晰；
- 禁止插值模糊；
- 适配高 DPI 屏幕；
- 不得因窗口缩放改变碰撞结果；
- 游戏逻辑坐标和显示坐标必须分离。

使用固定时间步长更新游戏逻辑，例如：

```text
60 Hz fixed timestep
```

渲染可以使用 `requestAnimationFrame`。

必须处理长帧和浏览器切换标签页后的时间累积，避免物体瞬移或穿墙。

---

## 3.2 游戏模式

主菜单必须至少包含：

```text
1 PLAYER
2 PLAYERS
DEMO
HELP
```

### 单人模式

- 玩家 1 独立游玩；
- 默认 3 条生命；
- 玩家死亡后在出生点重生；
- 生命耗尽后游戏结束。

### 本地双人模式

- 两名玩家同时游玩；
- 两名玩家拥有独立生命、分数和升级状态；
- 两人共同保护基地；
- 任意一名玩家仍有生命时，游戏可以继续；
- 两名玩家生命全部耗尽，或者基地被摧毁时游戏结束。

### DEMO 模式

- 由游戏 AI 自动控制玩家坦克；
- 至少能够自主移动、瞄准、开火和躲避障碍；
- DEMO 应可以连续运行至少 60 秒而不出现异常；
- 用户按任意主要操作键可以返回菜单。

---

# 四、完整游戏流程

必须实现以下状态机：

```text
BOOT
MAIN_MENU
HELP
STAGE_INTRO
PLAYING
PAUSED
PLAYER_RESPAWN
STAGE_CLEAR
SCORE_SUMMARY
GAME_OVER
VICTORY
```

完整流程：

```text
启动
→ 主菜单
→ 选择模式
→ 关卡介绍
→ 进入游戏
→ 消灭全部敌人
→ 分数统计
→ 下一关
→ 完成全部关卡
→ 胜利画面
```

失败条件：

- 基地被摧毁；
- 所有玩家均无剩余生命。

必须支持：

- 暂停；
- 恢复；
- 返回主菜单；
- 重新开始当前关卡；
- 开启或关闭音效；
- 清除存档；
- 浏览帮助和按键说明。

游戏暂停时：

- 游戏逻辑停止；
- 动画计时停止；
- 道具倒计时停止；
- 敌人停止；
- 子弹停止；
- 音频状态正确处理。

---

# 五、操作方式

## 玩家 1

```text
W / A / S / D：移动
Space：开火
```

## 玩家 2

```text
方向键：移动
Enter：开火
```

## 公共操作

```text
P：暂停或恢复
R：重新开始当前关卡
M：静音或恢复声音
Esc：返回菜单或关闭帮助
F2：显示或隐藏调试信息
```

要求：

- 阻止方向键和空格键导致网页滚动；
- 支持同时按下多个按键；
- 两名玩家输入不得互相覆盖；
- 窗口失去焦点时自动暂停；
- 重新获得焦点后不得自动恢复，必须由用户主动恢复；
- 输入系统必须区分“按住”和“刚刚按下”。

可以额外支持标准 Gamepad API，但键盘操作必须完整可用。

---

# 六、玩家坦克

每个玩家坦克必须拥有：

```text
位置
方向
移动速度
生命数量
分数
升级等级
当前子弹数量
射击冷却
重生保护状态
临时无敌状态
冰面滑动状态
是否存活
```

玩家坦克必须具有 4 个火力等级：

## Level 0：基础

- 普通子弹；
- 同时最多存在 1 发玩家子弹。

## Level 1：高速弹

- 子弹飞行速度提高。

## Level 2：双发

- 同时最多存在 2 发玩家子弹；
- 射击频率适度提升。

## Level 3：穿甲弹

- 可以摧毁钢墙；
- 保留前两级能力。

升级在当前生命内持续。

玩家死亡后，升级等级可以降低一级或重置为基础等级，但必须在 README 中明确采用的规则，并保证规则始终一致。

玩家出生和重生时必须拥有短暂保护罩。

保护罩期间：

- 可以移动和开火；
- 不受到敌方子弹伤害；
- 必须有清晰动画表现；
- 不能阻止基地受到攻击。

---

# 七、敌方坦克

必须实现至少 4 种敌方坦克：

## 7.1 基础型

- 普通移动速度；
- 普通射击速度；
- 1 点生命。

## 7.2 快速型

- 移动速度明显更快；
- 1 点生命。

## 7.3 火力型

- 射击频率更高；
- 子弹速度更高；
- 1 点生命。

## 7.4 装甲型

- 至少 4 点生命；
- 每次受到伤害后改变外观；
- 不同受损阶段必须可见。

敌人生成要求：

- 每关至少包含 20 辆敌方坦克；
- 屏幕中同时活动的敌人最多为 4 辆；
- 敌人从地图顶部的 3 个生成点出现；
- 生成时有短暂生成动画和保护期；
- 生成点被坦克占据时必须延迟生成；
- 不允许生成坦克和现有坦克重叠；
- 关卡信息栏必须显示剩余敌人数量。

部分敌人必须是闪烁的“道具携带者”。

道具携带者被玩家击毁后，应在地图有效位置生成随机道具。

---

# 八、敌人 AI

敌人不能只沿一个方向机械移动。

必须实现实际可玩的敌人 AI，包括：

- 在一定时间间隔后重新决定方向；
- 碰到障碍后改变方向；
- 避免长时间原地卡住；
- 有一定概率朝玩家移动；
- 有一定概率朝基地移动；
- 有一定概率进行随机巡逻；
- 在与玩家或基地大致对齐时提高开火概率；
- 根据坦克类型调整移动和射击行为；
- 不允许所有敌人采用完全相同的决策节奏；
- 使用确定性随机数生成器，以支持测试复现。

至少一种敌人应具有简单路径搜索或目标导向能力。

可以使用：

```text
BFS
A*
网格代价搜索
方向评分系统
```

但不得每帧对全部敌人执行高成本全图搜索。

必须避免：

- 敌人永久撞墙；
- 敌人持续左右抖动；
- 敌人无限堆叠；
- 敌人穿过彼此；
- 敌人穿过玩家；
- 敌人穿过不可通行地形。

---

# 九、地图地形

必须实现以下地形：

## 9.1 空地

- 坦克和子弹均可通过。

## 9.2 砖墙

- 阻挡坦克；
- 阻挡子弹；
- 可以被普通子弹摧毁；
- 砖墙应支持局部破坏，而不是整块只能全部消失；
- 建议将一个 16×16 砖块划分为四个 8×8 子区域；
- 子弹必须根据撞击位置破坏对应区域。

## 9.3 钢墙

- 阻挡坦克；
- 阻挡普通子弹；
- 普通子弹撞击时产生不同音效或火花；
- 只有最高等级玩家穿甲弹可以摧毁；
- 敌方普通子弹不能摧毁。

## 9.4 水面

- 坦克不能通过；
- 子弹可以通过；
- 具有简单水面动画。

## 9.5 草丛

- 不阻挡坦克和子弹；
- 草丛必须绘制在坦克上层；
- 坦克进入草丛后应被部分或完全遮挡。

## 9.6 冰面

- 坦克可以通过；
- 松开方向键或改变方向后仍会短暂滑行；
- 不得导致坦克穿墙；
- 不同坦克的滑行状态独立。

## 9.7 基地

- 位于地图底部区域；
- 被砖墙或钢墙区域包围；
- 玩家和敌人均不能穿过；
- 任意阵营子弹均可以摧毁基地；
- 玩家误射基地也会导致游戏结束；
- 基地摧毁必须有明显动画和音效。

---

# 十、碰撞和物理规则

必须实现可靠的连续游戏碰撞。

至少处理：

```text
坦克与地图边界
坦克与砖墙
坦克与钢墙
坦克与水面
坦克与基地
坦克与坦克
子弹与地图边界
子弹与砖墙
子弹与钢墙
子弹与坦克
子弹与基地
敌对阵营子弹与子弹
坦克与道具
```

要求：

- 坦克不得穿墙；
- 坦克不得互相重叠；
- 子弹高速移动时不得明显穿透薄墙；
- 使用扫掠检测、分段移动或足够可靠的连续碰撞方案；
- 碰撞结果不得依赖显示帧率；
- 同一帧发生多个碰撞时必须有确定的处理顺序；
- 已销毁对象不得在同一帧继续造成重复伤害；
- 所有对象都必须在合理时间被清理，避免内存持续增长。

子弹规则：

- 玩家子弹可以摧毁敌人；
- 敌方子弹可以摧毁玩家；
- 同阵营子弹默认不互相伤害；
- 敌对阵营子弹相撞后互相抵消；
- 玩家子弹击中另一名玩家时不扣生命，但造成约 0.5 秒短暂失控或停顿；
- 子弹击中墙体、坦克或基地后必须正确销毁；
- 不允许一发普通子弹同时穿透多个目标。

---

# 十一、道具系统

必须实现以下 6 种道具：

## 11.1 星星

- 玩家火力提升一级；
- 最高为 Level 3。

## 11.2 防护罩

- 玩家获得约 10 秒无敌；
- 结束前应有闪烁提示。

## 11.3 时钟

- 所有敌方坦克和敌方子弹冻结约 10 秒；
- 新生成敌人也应受到剩余冻结时间影响；
- 玩家、玩家子弹和道具不冻结。

## 11.4 铲子

- 基地周围的防御墙临时变成钢墙；
- 持续约 15 至 20 秒；
- 结束前闪烁；
- 结束后恢复激活道具前的基地墙状态；
- 多次拾取时应合理刷新持续时间，而不是破坏墙体状态。

## 11.5 手雷

- 立即摧毁当前屏幕上的全部活动敌人；
- 正确计入击杀分数；
- 不摧毁等待生成的敌人；
- 不伤害玩家和基地。

## 11.6 额外生命

- 拾取玩家增加 1 条生命。

道具要求：

- 同一时间最多存在一个未拾取道具；
- 新道具出现时可以替换旧道具；
- 道具具有闪烁或呼吸动画；
- 道具只能生成在合法、可到达且没有实体重叠的位置；
- 道具经过合理时间后自动消失；
- 拾取时播放提示音并显示文字反馈。

---

# 十二、计分系统

建议基础分数：

```text
基础型敌人：100
快速型敌人：200
火力型敌人：300
装甲型敌人：400
拾取道具：500
```

必须实现：

- 玩家独立分数；
- 最高分；
- 每关击杀统计；
- 按敌人类型统计击杀数量；
- 关卡结束统计页面；
- 总分累积；
- 最高分保存到 localStorage。

双人模式必须明确每个敌人由哪名玩家击毁，并将分数计入对应玩家。

可以实现额外生命分数阈值，但规则必须固定，并写入 README。

---

# 十三、关卡系统

必须包含：

```text
35 个可玩的原创关卡
```

要求：

- 35 个关卡必须彼此不同；
- 不得只复制同一关并修改编号；
- 不得直接复制经典商业游戏的原始地图；
- 可以使用静态关卡数据；
- 也可以使用固定种子的确定性关卡生成；
- 同一版本、同一关卡每次加载时必须保持相同布局；
- 每关包含基地、玩家出生点和 3 个敌人生成点；
- 每关至少存在一条从敌人区域通往基地附近的有效路径；
- 玩家出生点不能封死；
- 敌人出生点不能封死；
- 不可通行地形比例必须合理；
- 不允许生成无法完成的关卡；
- 难度应随关卡总体上升；
- 后期逐渐增加钢墙、水面、冰面和装甲敌人；
- 每关必须定义 20 辆敌人的类型构成和生成顺序；
- 完成第 35 关后进入 VICTORY 状态。

必须编写关卡验证器，对全部 35 个关卡检查：

```text
尺寸正确
字符或 Tile ID 合法
基地存在且唯一
出生点数量正确
生成点没有阻挡
玩家出生点没有阻挡
存在有效路径
敌人总数正确
关卡编号唯一
地图数据不是完全重复
```

---

# 十四、图形和动画

禁止使用外部素材。

必须通过 Canvas 绘制：

- 不同玩家坦克；
- 4 种敌方坦克；
- 坦克履带动画；
- 炮塔或方向变化；
- 子弹；
- 砖墙；
- 钢墙；
- 水面动画；
- 草丛；
- 冰面；
- 基地正常和被摧毁状态；
- 6 种道具；
- 生成动画；
- 保护罩；
- 小型爆炸；
- 大型爆炸；
- 火花；
- 得分浮字；
- 暂停覆盖层；
- 关卡介绍；
- 游戏结束；
- 胜利画面。

不同阵营和不同坦克类型必须能够通过颜色、轮廓和细节区分。

不得使用 Emoji 作为主要游戏图形。

必须保证：

- Canvas 缩放后仍为清晰像素风；
- 草丛遮挡层级正确；
- 爆炸和实体销毁同步；
- 画面中不存在长期残留的无效动画对象。

---

# 十五、音频系统

使用 Web Audio API 合成音效，不得加载音频文件。

至少实现：

```text
菜单确认
关卡开始
玩家移动或引擎
玩家射击
敌方射击
子弹撞砖墙
子弹撞钢墙
坦克受击
小型爆炸
大型爆炸
拾取道具
玩家死亡
关卡完成
游戏结束
最终胜利
```

要求：

- 浏览器首次用户交互后再创建或恢复 AudioContext；
- 支持静音；
- 静音状态保存；
- 不允许创建无限数量的 AudioContext；
- 连续射击时控制音量，避免爆音；
- 页面隐藏或暂停时正确处理循环音效；
- 音频功能不可用时，游戏仍然可以正常运行。

---

# 十六、用户界面

右侧信息栏至少显示：

- 剩余敌人；
- 当前关卡；
- 玩家 1 生命；
- 玩家 1 分数；
- 玩家 2 生命和分数，双人模式时显示；
- 当前最高分；
- 静音状态；
- 暂停状态。

主菜单显示：

- 游戏标题；
- 单人模式；
- 双人模式；
- DEMO；
- HELP；
- 最高分；
- 已解锁最高关卡；
- 清除存档入口。

帮助页面显示：

- 完整按键；
- 地形说明；
- 道具说明；
- 胜负条件；
- 暂停和静音方式。

使用 localStorage 保存：

```text
最高分
最高已解锁关卡
音效开关
音量
最后选择的模式
版本化存档结构
```

必须处理：

- localStorage 不可用；
- 存档 JSON 损坏；
- 旧版本存档缺少字段；
- 用户清除存档。

损坏存档不得导致游戏无法启动。

---

# 十七、代码架构

不得把全部逻辑塞进一个巨大的 JavaScript 文件。

建议结构：

```text
tank-battle-benchmark/
├─ index.html
├─ styles.css
├─ package.json
├─ server.mjs
├─ README.md
├─ ARCHITECTURE.md
├─ QA_REPORT.md
├─ BENCHMARK_REPORT.md
├─ src/
│  ├─ main.js
│  ├─ config.js
│  ├─ game.js
│  ├─ state-machine.js
│  ├─ loop.js
│  ├─ renderer.js
│  ├─ input.js
│  ├─ audio.js
│  ├─ storage.js
│  ├─ rng.js
│  ├─ collision.js
│  ├─ map.js
│  ├─ levels.js
│  ├─ level-validator.js
│  ├─ ai.js
│  ├─ entities/
│  │  ├─ tank.js
│  │  ├─ player.js
│  │  ├─ enemy.js
│  │  ├─ bullet.js
│  │  ├─ powerup.js
│  │  └─ effects.js
│  └─ ui/
│     ├─ menu.js
│     └─ hud.js
└─ tests/
   ├─ collision.test.js
   ├─ level-validator.test.js
   ├─ powerups.test.js
   ├─ damage.test.js
   ├─ spawning.test.js
   ├─ state-machine.test.js
   └─ deterministic-simulation.test.js
```

可以调整文件结构，但必须保证职责清晰。

架构要求：

- 游戏规则与 Canvas 渲染分离；
- 核心模拟逻辑可以在 Node.js 中测试；
- 不允许核心碰撞测试依赖真实浏览器；
- 使用对象池或可靠清理机制管理子弹和特效；
- 禁止通过到处修改全局变量维持状态；
- 使用集中式配置管理主要参数；
- 随机数必须支持显式 seed；
- 关键代码包含必要注释；
- 不得存在未使用的大段死代码；
- 不得留下 `TODO`、`FIXME`、占位函数或空实现；
- 不得通过隐藏错误或捕获所有异常来伪造稳定性；
- 不得使用 `eval` 或 `new Function`。

---

# 十八、调试和自动评估接口

在 URL 包含：

```text
?debug=1
```

时，暴露：

```javascript
window.__TANK_BATTLE_TEST__
```

至少包含：

```javascript
{
  version,
  startGame(options),
  resetGame(),
  pauseGame(),
  resumeGame(),
  stepFrames(frameCount),
  setPlayerInput(playerId, inputState),
  getSnapshot(),
  spawnEnemy(type, position),
  spawnPowerUp(type, position),
  damageTank(entityId, amount),
  destroyBase(),
  clearActiveEnemies()
}
```

`getSnapshot()` 必须返回可 JSON 序列化的数据，至少包含：

```text
当前游戏状态
模式
关卡
帧编号
随机种子
玩家状态
敌人状态
子弹状态
道具状态
基地状态
计时器
剩余敌人
分数
活动对象数量
```

正常模式不得依赖这些调试接口工作。

---

# 十九、自动化测试

使用 Node.js 自带的：

```javascript
node:test
```

不得引入 Jest、Vitest、Mocha 或其他依赖。

至少编写 25 个有效测试用例，覆盖：

## 碰撞

- 坦克不能穿过砖墙；
- 坦克不能穿过钢墙；
- 坦克不能进入水面；
- 坦克可以进入草丛；
- 坦克可以进入冰面；
- 子弹可以穿过水面；
- 普通子弹摧毁砖墙局部；
- 普通子弹不能摧毁钢墙；
- 穿甲弹可以摧毁钢墙；
- 敌对子弹相撞后抵消；
- 高速子弹不会明显穿墙。

## 伤害

- 玩家子弹击毁基础敌人；
- 装甲敌人需要多次命中；
- 敌方子弹击毁无保护玩家；
- 保护罩阻止玩家伤害；
- 基地被任意阵营子弹击毁；
- 已销毁实体不会重复计分。

## 道具

- 星星提高等级但不超过上限；
- 防护罩到期；
- 时钟冻结敌人并正确恢复；
- 铲子改变并恢复基地墙；
- 手雷只消灭当前活动敌人；
- 额外生命增加生命。

## 游戏流程

- 所有敌人消灭后进入关卡完成；
- 基地摧毁进入游戏结束；
- 双人模式中一名玩家死亡不立即结束；
- 两名玩家都没有生命时结束；
- 第 35 关完成后进入胜利状态；
- 暂停期间计时器不推进。

## 关卡验证

- 35 个关卡全部通过验证；
- 不存在完全重复关卡；
- 每关基地唯一；
- 每关出生点合法；
- 每关敌人总数正确；
- 每关存在有效路径。

## 确定性模拟

以固定 seed 模拟至少：

```text
3600 个逻辑帧
```

验证：

- 不抛出异常；
- 不出现 NaN；
- 不出现 Infinity；
- 活动对象数量不无限增长；
- 坦克不会跑出地图；
- 子弹不会永久存在；
- 已销毁对象会被清理；
- 相同 seed 和输入得到相同结果。

`npm test` 必须返回非零退出码表示测试失败，返回零表示测试通过。

禁止编写永远为真的假测试。

禁止通过跳过测试、注释断言或捕获断言错误伪造通过。

---

# 二十、实际运行验证

完成编码后必须：

1. 执行 `npm test`；
2. 修复全部失败测试；
3. 执行 `npm start`；
4. 验证首页能够加载；
5. 验证主菜单显示；
6. 验证单人模式可以进入；
7. 验证玩家可以移动和射击；
8. 验证敌人能够生成和行动；
9. 验证暂停和恢复；
10. 验证重新开始；
11. 验证游戏结束；
12. 验证双人输入；
13. 验证 DEMO 模式；
14. 检查控制台是否存在未捕获异常。

如果环境具有浏览器自动化或截图能力，再生成：

```text
artifacts/main-menu.png
artifacts/single-player.png
artifacts/two-player.png
artifacts/stage-clear.png
```

如果没有浏览器或截图工具，应在报告中标记：

```text
BROWSER_VISUAL_CHECK = TOOL_LIMITED
```

但仍必须完成代码测试和静态服务器验证。

---

# 二十一、文档交付

## README.md

必须包含：

- 游戏说明；
- 技术栈；
- 项目结构；
- 启动命令；
- 测试命令；
- 完整按键；
- 游戏规则；
- 4 种敌人；
- 7 类地形；
- 6 种道具；
- 火力升级规则；
- 玩家死亡后的升级规则；
- 存档说明；
- 调试模式使用方式；
- 已知限制。

## ARCHITECTURE.md

必须说明：

- 主循环；
- 固定时间步长；
- 状态机；
- 实体管理；
- 碰撞系统；
- 地图系统；
- AI 系统；
- 音频系统；
- 存档系统；
- 确定性随机数；
- 测试架构。

## QA_REPORT.md

必须逐项记录：

```text
测试项目
执行方式
实际结果
是否通过
对应证据
```

不得只写“已测试”。

## BENCHMARK_REPORT.md

必须按照以下评分标准严格自评，并为每项提供文件、函数、测试或截图证据。

### 评分标准：总分 100

```text
可启动性和稳定性：15
核心碰撞与战斗机制：20
完整游戏流程：15
敌人 AI 和关卡系统：15
双人、道具和升级系统：10
视觉、动画、音效和 UI：10
代码架构和可维护性：5
自动化测试和确定性：10
```

不得在没有证据时给满分。

---

# 二十二、禁止事项

以下情况均不允许：

- 只输出方案；
- 只输出伪代码；
- 只做一张静态画面；
- 只做玩家坦克而没有敌人；
- 只做敌人但没有基地；
- 没有碰撞；
- 没有完整关卡流程；
- 用随机色块代替所有界面而不做基本可辨识图形；
- 只做一个关卡；
- 将 35 个关卡全部设为相同地图；
- 使用外部游戏图片或音乐；
- 使用第三方框架；
- 依赖互联网；
- 把所有代码塞进一个不可维护的大文件；
- 用 `alert()` 代替正式 UI；
- 留下空函数；
- 留下大量 TODO；
- 省略自动化测试；
- 伪造测试结果；
- 声称运行成功但实际上没有执行；
- 因一个功能困难而停止整个项目。

---

# 二十三、状态判定

只有同时满足以下条件，才可以判定：

```text
DELIVERY_STATUS = PASS
```

PASS 条件：

- `npm start` 可以启动；
- `npm test` 全部通过；
- 单人模式可玩；
- 双人模式可玩；
- DEMO 可运行；
- 35 个关卡存在且通过验证；
- 4 种敌人完整；
- 7 类地形完整；
- 6 种道具完整；
- 玩家升级完整；
- 基地机制完整；
- 关卡切换完整；
- 游戏结束和最终胜利完整；
- 没有外部依赖；
- 没有关键占位实现；
- 没有已知阻断性错误。

游戏能够运行，但缺少一项或多项重要功能时：

```text
DELIVERY_STATUS = PARTIAL
```

项目无法运行、只有框架、核心机制缺失或测试无法执行时：

```text
DELIVERY_STATUS = FAIL
```

工具限制必须单独记录，不得把工具限制伪装成 PASS。

---

# 二十四、最终回复格式

完成全部工作后，最终回复必须严格包含：

```text
DELIVERY_STATUS = PASS / PARTIAL / FAIL
GAME_RUNTIME = PASS / FAIL / TOOL_LIMITED
UNIT_TESTS = PASS / FAIL
LEVEL_VALIDATION = PASS / FAIL
SINGLE_PLAYER = PASS / FAIL
TWO_PLAYER = PASS / FAIL
DEMO_MODE = PASS / FAIL
CORE_MECHANICS = PASS / PARTIAL / FAIL
BROWSER_CONSOLE_ERRORS = 0 / 数量 / TOOL_LIMITED
EXTERNAL_DEPENDENCIES = 0 / 数量
PLACEHOLDER_IMPLEMENTATIONS = 0 / 数量
TOTAL_TESTS = 实际数量
PASSED_TESTS = 实际数量
FAILED_TESTS = 实际数量
BENCHMARK_SCORE = 0–100
```

然后提供：

```text
项目目录
启动命令
测试命令
主要文件列表
实际完成的功能
未完成的功能
测试证据摘要
已知问题
```

不要在最终回复中粘贴全部源码，除非当前环境完全无法创建文件。

不要仅凭代码看起来正确就声称 PASS。

现在直接开始创建并验证项目。
```

---

## 使用建议

为了让不同模型的结果更可比，测试时应：

- 使用同一个空目录；
- 使用相同 Node.js / 浏览器环境；
- 对每个模型只发送这一份提示词；
- 不追加人工修正提示；
- 记录模型总耗时、Token/费用、最终 `BENCHMARK_SCORE`、测试通过率以及人工试玩结果。
